{
  "name": "manabo",
  "private": true,
  "version": "0.0.0",
  "description": "Manabo (学ぼ) — B-JET Hiragana & Katakana Learning Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "drizzle-orm": "^0.36.4"
  }
}
